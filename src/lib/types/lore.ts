
// This file can be used for shared Zod schemas or TypeScript types related to lore
// that might be used across multiple components or flows, if necessary.
// For now, the primary schemas are defined within their respective flow files
// for better co-location (e.g., ResearchRealWorldTopicOutput in research-real-world-lore-flow.ts).

// Example of a shared type if needed:
// export interface LoreTag {
//   id: string;
//   name: string;
// }

// No shared types strictly needed for the current implementation as schemas are self-contained in flows.
// This file exists as a placeholder for future expansion.
export {};
